<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /api/courses/
    # send everything to index.php while preserving path info
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L,QSA]
</IfModule>